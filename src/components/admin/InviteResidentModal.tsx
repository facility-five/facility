import { InviteResidentModal as BaseInviteResidentModal } from "@/components/manager/InviteResidentModal";

// Re-export the same component for Admin use
// The component is generic enough to work in both Manager and Admin contexts
export const InviteResidentModal = BaseInviteResidentModal;